<!DOCTYPE html>
<html>
	<%- include('../partials/head'); %>
	<head>
		<link rel="stylesheet" href="/stylesheets/style.css" />
	</head>
	<body>
		<%- include('../partials/header'); %>

		<form action="/login/" class="input-form" method="post">
			<div class="input-container">
				<h1 class="input-title"><%= pageTitle %> here</h1>

				<div class="username-row">
					<label class="username-label" for="Username">Username:</label>
					<div>
						<input required class="username-input" name="username" id="Title" />
					</div>
				</div>

				<div class="password-row">
					<label class="password-label" for="Password">Password:</label>
					<div>
						<input
							required
							class="password-input"
							name="password"
							id="password"
						/>
					</div>
				</div>

				<div class="buttons-row">
					<input type="submit" value="Login" class="login-btn button" />
					<a class="cancel-btn button" href="/">Cancel</a>
				</div>
			</div>
		</form>

		<%- include('../partials/footer'); %>
	</body>
</html>
